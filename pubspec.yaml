name: 'application'
description: "Clean architecture approach example"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  data:
    path: ./layers/data

  domain:
    path: ./layers/domain

  presentation:
    path: ./layers/presentation

flutter:
  uses-material-design: true